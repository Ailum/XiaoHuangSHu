server:
  port: 8080

spring:
  application:
    name: xiaohuangshu-auth
  profiles:
    active: dev
  config:
    import: optional:nacos:xiaohuangshu-auth.yaml
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        file-extension: yaml
        refresh-enabled: true
        prefix: ${spring.application.name}
        namespace: xiaohuangshu
      discovery:
        enabled: true #启用服务发现
        group: DEFAULT_GROUP
        namespace: xiaohuangshu
        server-addr: 127.0.0.1:8848

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml

sa-token:
  token-name: satoken
  timeout: 2592000
  active-timeout: -1
  is-concurrent: true
  is-share: true
  token-style: uuid
  is-log: true

alarm:
  type: sms

rate-limit:
  api:
    limit: 100
